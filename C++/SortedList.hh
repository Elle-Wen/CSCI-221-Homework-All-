using namespace std;
#include <string>
/*
class Node{
	public:
		Node();
		~Node();
		string data;
		Node* next; 
};
class SortedList{
	public:
		SortedList();
		~SortedList();
		Node* head;
		string sorting_type;
		SortedList* deep_copy() const; 
		void add_element(string element);
		void remove_element(string element); 
		void remove_all(); 		
		bool is_sorting_method_length()const;
		bool is_sorting_method_ASCII()const; 
		int length()const;
		string* only_data()const; 
		string value_at_position(int position)const;
		void change_sorting_to_length(); 
		void change_sorting_to_ASCII(); 
	private:
	protected:
};
*/